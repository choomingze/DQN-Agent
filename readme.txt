1.The code is coded in Google Colab and saved as .pynb file
2. Donwload the file and import the code in Google Colab
3. Results of the project is already provided when imported
4. To run the same model please add the checkpoints file provided to the 
to the folder and load the dqn model by running the code in "Load saved model"
cell.
5. Change the checkpoint file name if wanted to load the other model in DqnAgent class
at the __init__ function. (the line is commented)